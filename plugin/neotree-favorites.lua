-- neotree-favorites.nvim - Flat Favorites source for neo-tree
-- This file is automatically loaded by Neovim when the plugin is installed

-- Prevent loading the plugin twice
if vim.g.loaded_neotree_favorites then
  return
end
vim.g.loaded_neotree_favorites = 1

-- The actual source registration happens when neo-tree loads
-- See lua/neotree-favorites/init.lua for the source implementation
